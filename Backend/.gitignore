# Secrets and key material -- never commit these
.env
.env.*
!.env.example
secret.key
*.key
*.enc

# Runtime data (default CHANAKYA_DATA_DIR): incident DB, snapshots, clips,
# enrolled face photos, face encodings, zones, the report key and reports.
database/
Chanakya_Report_*.pdf
*.pkl
*.npz

# Virtual environment
venv/
.venv/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.ruff_cache/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Evaluation fixture clip (regenerated by tools/make_fixture.py; the labels are committed)
eval/fixtures/*.mp4
