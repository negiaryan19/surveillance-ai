# Project Chanakya backend

Flask API and the always-on detection engine. Setup, configuration, the API reference, tests and the
security notes are in the [root README](../README.md).

Quick start: `python3.11 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && python web/app.py`
