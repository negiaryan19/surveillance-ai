# Project Chanakya backend — use Python 3.11 (dlib/torch wheels are unreliable on 3.13+)
#   python3.11 -m venv venv && source venv/bin/activate && pip install -r requirements.txt

# Web
flask>=3.0,<4
flask-cors>=4.0,<7
python-dotenv>=1.0,<2
requests>=2.34.2,<3

# Vision
numpy>=1.26,<3
opencv-python>=4.9,<5
ultralytics>=8.2,<9
lap>=0.5.12

# Face ID / liveness / emotion (dlib needs cmake: `brew install cmake`).
# setuptools stays <81 because face_recognition_models imports pkg_resources, which
# setuptools 81+ removed. pip-audit flags PYSEC-2026-3447 on this pin; the advisory
# concerns installing untrusted packages, which this service never does at runtime.
# The pin goes away with the ONNX face stack planned in ROADMAP.md phase 2.
setuptools<81
face_recognition>=1.3,<2

# Face enrollment uploads (untrusted image bytes are decoded with Pillow, never cv2.imdecode)
Pillow>=10.3,<13

# Reports
fpdf2>=2.7,<3
cryptography>=42

# Tests
pytest>=9,<10
