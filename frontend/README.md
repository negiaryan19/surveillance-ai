# Project Chanakya dashboard

React 19 + Vite dashboard for the Chanakya backend. Setup, configuration (`VITE_API_URL`) and usage
are in the [root README](../README.md).

Quick start: `npm ci && npm run dev -- --host 127.0.0.1`, then open `http://127.0.0.1:5173`.
